default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>

development:
  <<: *default
  database: mountain_time_development

test:
  <<: *default
  database: mountain_time_test

production:
  url: <%= ENV.fetch('DATABASE_URL') { '' } %>
